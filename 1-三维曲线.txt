1.plot3用来绘制三维曲线，它就是plot的一个扩展
plot3(x,y,z);

2.三维曲面
平面网格坐标矩阵的生成
利用meshgrid函数生成；
x=a:dx:b;   //n
y=c:dy:d;   //m
[X,Y]=meshgrid(x,y);   //注意此时得到的是一个mxn大小的，而不是nxm;
语句执行后，所得到的网格坐标矩阵和上法，相同，当x=y时，可以写成meshgrid(n);


三维曲面绘制：
Matlab提供了mesh函数和surf函数来绘制三维曲面图。mesh函数用来绘制三维网格图，而surf用来绘制三维曲面图，各线条之间的补面用颜色填充。其调用格式为：
mesh（x，y，z，c）
surf（x，y，z，c）
一般情况下，x，y，z是维数相同的矩阵，x，y是网格坐标矩阵，z是网格点上的高度矩阵，c用于指定在不同高度下的颜色范围。c省略时，Matlab认为c=z，也即颜色的设定是正比于图形的高度的。这样就可以得到层次分明的三维图形。当x，y省略时，把z矩阵的列下标当作x轴的坐标，把z矩阵的行下标当作y轴的坐标，然后绘制三维图形。当x，y是向量时，要求x的长度必须等于z矩阵的列，y的长度必须等于必须等于z的行，x，y向量元素的组合构成网格点的x，y坐标，z坐标则取自z矩阵，然后绘制三维曲线。


3.高斯函数三维曲面的绘制
首先生成网格坐标矩阵，然后分别对x和y进行平方操作，注意，这里x和y必须逐数据操作，即要在矩阵后加上.；


4.三维曲面绘制选项
shading interp  //remove edge lines a smoot colors

5.自带三维曲面函数
[x,y,z]=peaks;  //绘制山峰
[x,y,z]=sphere(n);  //绘制球面